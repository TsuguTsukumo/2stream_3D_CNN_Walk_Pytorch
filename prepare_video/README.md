# `prepare_video`

Canonical video preprocessing package.

Use the module entrypoint:

```bash
python -m prepare_video --help
```

Available commands:

- `preprocess`
- `split`
- `balance`
- `rename`
